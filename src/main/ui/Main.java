package ui;

import model.Inventory;
import model.recipe.RecipeBook;

// main class  that allows the program to be run
public class Main {

    // EFFECTS: main function that allows project app to be run
    public static void main(String[] args) {
        ProjectApp mainApp = new ProjectApp();
        mainApp.runApp();
    }
}
